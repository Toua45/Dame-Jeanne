    <div class="sticky__alert">
        <!-- Intégration d'un fragment d'un controller dans notre template --->
    {{ render(controller(
        'App\\Controller\\HeaderMessageController::getHeaderMessage'
    )) }}
</div>
<nav class="navbar navbar-expand-lg container">
    <div class="ml-1 row d-flex justify-content-around align-items-center w-100">
        <div class="col-lg-12 col-xs-1">
            <div class="d-flex justify-content-center">
                <div class="logo">
                    <a href="{{ path('home') }}">
                        <img src="{{ asset('build/logo.webp') }}" class="logo-responsive d-inline-block align-middle"
                             alt="logo jeanne">
                    </a>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="navbar-light">
                <button class="mb-4 navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="true" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
        </div>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav d-flex justify-content-around align-items-center w-100">
                <li class="nav-item d-flex">
                    <a class="ml-3 navbar-brand" href="/"><span class="navbar-link">Accueil</span></a>
                </li>
                <li class="nav-item d-flex">
                    <a href="{{ path('product_index') }}" class="nav-link"><span class="nav-effect">Nos produits</span></a>
                </li>
                <li class="nav-item d-flex">
                    <a href="{{ path('presentation_index') }}" class="nav-link"><span
                                class="nav-effect">L'esprit Dame Jeanne</span></a>
                </li>
                <li class="nav-item d-flex">
                    <a href="{{ path('blog_index') }}" class="nav-link"><span
                                class="nav-effect">Les rendez-vous</span></a>
                </li>
                <li class="nav-item d-flex">
                    <a href="{{ path('pedago_index') }}" class="nav-link"><span
                                class="nav-effect">Le dico</span></a>
                </li>
                <li class="nav-item d-flex align-self-center">

                    {% if app.user %}
                        <a href="{{ path('admin_home') }}" class="nav-link"><span
                                    class="nav-effect">Page administration</span></a>
                </li>
                <li class="nav-item d-flex align-self-center">
                        <a href="{{ path('app_logout') }}" class="nav-link"><span
                                    class="nav-effect">Se déconnecter</span></a>
                </li>
                <li class="nav-item d-flex">
                    {% else %}
                        <a href="{{ path('app_login') }}" class="nav-link">
                            <span class="nav-effect">Connexion</span></a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>
