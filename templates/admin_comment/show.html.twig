{% extends 'layout_admin.html.twig' %}

{% block title %}Commentaire n°{{ comment.id }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center m-5">Commentaire de {{ comment.author }}</h1>
        <div class="admin-section pt-5 pb-5 px-5">
            <table class="table">
                <tbody>
                <tr>
                    <th>Auteur</th>
                    <td>{{ comment.author }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ comment.date ? comment.date|date('d/m/Y H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Commentaire</th>
                    <td>{{ comment.content }}</td>
                </tr>
                </tbody>
            </table>
        </div>
        <div class="d-flex justify-content-around">
            <a href="{{ path('admin_comment_index') }}" class="btn btn-dark mt-5">
                <i class="far fa-arrow-alt-circle-left"></i> Liste des commentaires</a>
            {{ include('admin_comment/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
