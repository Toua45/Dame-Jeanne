{{ form_start(form) }}
        {# {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        {{ form_row(form.content) }}
        {{ form_row(form.category) }}
        {{ form_row(form.author) }}
        {{ form_row(form.date) }} #}
            <p id="displayVich"></p>
        {# {{ form_row(form.imageFile) }} #}
        {{ form_widget(form) }}
        <div class="flex d-flex justify-content-around">
            <button class="btn btn-outline-danger mt-5 mb-5">{{ button_label|default('Valider') }}<span uk-icon="check"></span></button>
        </div>
{{ form_end(form) }}
