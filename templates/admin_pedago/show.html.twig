{% extends 'layout_admin.html.twig' %}

    {% block title %}Quel est ce mot ?{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center m-5">Le mot en question : {{ pedago.title }}</h1>
        <div class="admin-section-pedago pt-5 pb-5 px-5">
            <table class="table">
                <tbody>
                <tr>
                    <th>Définition</th>
                    <td>{{ pedago.content|striptags|raw }}</td>
                </tr>
                <tr>
                    <th>Lien</th>
                    <td>{{ pedago.link }}</td>
                </tr>
                </tbody>
            </table>
            <div class="d-flex justify-content-center">
                <a href="{{ path('admin_pedago_edit', {'id': pedago.id}) }}"
                   class="btn btn-outline-danger"><span uk-icon="pencil"></span>
                    Editer
                </a>
            </div>
        </div>
        <div class="d-flex justify-content-around">
            <a href="{{ path('admin_pedago_index') }}" class="btn btn-dark mt-5"><span uk-icon="home"></span>
                Retour
            </a>
            {{ include('admin_pedago/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
